extends base

include mixins/crearEditarProyecto


block content
    .contenido-principal
        h1 #{nombrePagina}
        if errores
            each error in errores
                .alerta.error= error.texto

        +crearEditarProyecto(proyecto)